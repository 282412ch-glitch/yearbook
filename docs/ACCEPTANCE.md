# 100% 节点验收记录

更新时间：2026-09-12（Asia/Shanghai）。实际 Git 工作区：`D:\项目文件夹\yearbook`。本轮从已有 75% 节点继续，补全未来信、年册排序、统一导出排版与离线字体，并执行最终回归。

自动化全部使用独立临时资料目录，没有把测试记录、照片、信件或模型配置混入正式 `data`。测试生成物和回执保存在被 Git 忽略的 `test-results`。真实模型结论单独列出，不将模拟结果算作真实生成质量。

## 项目脚本与 Windows

验证环境：Windows、Node.js **24.14.1**、npm **11.17.0**。要求 Node 24 LTS（`>=24.14.1 <25`），提交依赖锁和迁移 001–005。

| 检查 | 实际结果 | 范围与证据 |
|---|---|---|
| `npm run typecheck` | 通过 | 前后端、共享包和测试 |
| `npm run build` | 通过 | tsup ESM、Vite 前端及本地字体；`test-results/final-build.log` |
| `npm test` | **18 文件、149 项通过** | SQLite、媒体、迁移、备份、双协议、AI、未来信、排序、导出与异常任务 |
| `npm run test:e2e` | 业务流程通过，退出码 0 | `test-results/2026-09-12T13-53-48-977Z-65276/`；4 次截图回退单独记录 |
| `npm start` | 通过 | 生产 UI/API、端口冲突避让、认证停止、重启保留 |
| `npm run dev` | 通过 | Vite 页面/代理与后端；后端停止后开发前端退出 |

`node scripts/verify-runtime.mjs` 在完整中文空格临时项目路径内独立构建并完成 **11 步**：生产/开发启动、端口冲突、错误令牌拒绝、无关服务保护、停止与重启保留、重复启动复用，以及原 `Start-Yearbook.cmd` / `Stop-Yearbook.cmd`（含 `pause` 和重复停止）。结束后只清理本次进程、依赖链接和临时项目。证据：`test-results/runtime-final.json`。

## 核心与接口结果

- 本地日期、闰日、往年补记、日期待补、一句话与纯照片记录；中文关键词和人物/标签/年月组合搜索。
- 原图哈希去重、EXIF 方向、阅读图/缩略图；外部原文件移动、共享引用解绑和重启后仍可读取。
- 记录删除恢复、人物/标签/补记关系、盲盒零条/一条/多条及短期去重、用户标记的第一次。
- 多本同年年册、章节/块/照片排序、来源、空册保存、版本快照恢复。排序专项 7 项包含 SQLite 保存和关闭重开。
- Responses / Chat Completions 分别处理 URL 前缀、完整地址、模型、图片/工具、SSE 与用量；四项能力独立验证。无效地址、鉴权、模型无权限、超时、限流、服务错误、能力缺失、截断和异常响应有分类。
- 标题、整理、补问、月报、分月全年稿、受限 Agent 与能力降级；原文快照、逐段来源、独立草稿、重新生成和明确采用保留手动版本。
- 任务取消/超时/重试、工具上限、执行代次、重启识别和阶段复用；迟到结果不覆盖下一次执行、不重复保存。
- 备份一致性快照、文件哈希、SQLite integrity/FK、记录/照片/年册/AI/信件关联；新中文空格目录恢复、恢复前备份和失败回滚。恢复期间迟到请求被拒绝，凭据引用与能力在副本中清空。
- 旧迁移备份按原结构验证后升级至 005；不改写已应用的 001–004。

## 给未来的信

未来信专项 **16 项通过**，包括真实接口与独立目录备份恢复：草稿/照片/顺序、封存校验、不可修改、按本地日期到期、首页提醒、显式拆阅和首次阅读幂等、删除恢复，以及损坏备份拒绝。

未到期详情只返回信封，不返回正文和照片列表；独占信件照片不能凭媒体 ID 读取或重新关联。普通记录搜索、盲盒和 AI 工具不访问信件，既有记录/年册/草稿的共享照片继续可用。完整原文件重新上传并通过哈希核对后可获得短期复用授权，封存、重启或恢复换库会撤销；没有持久权限绕过。

实际浏览器已走通写信/照片、未提交草稿恢复、取消和确认封存、重启后的到期提醒、拆阅及删除恢复。程序关闭期间不依赖计时器。封存是应用内日期约束，数据库和备份没有加密，不承诺抵御直接读取本机文件或修改系统日期。

## 实际浏览器回归

入口为 `scripts/e2e.mjs`、`e2e-flows.mjs`、`e2e-final-flows.mjs`，使用 Tabbit 与独立生产服务、本机确定性模拟模型。

最终完整业务回归通过，退出码 0。证据目录为 `test-results/2026-09-12T13-53-48-977Z-65276/`，含逐步回执、`summary.json`、真实导出 ZIP/HTML/PDF 与截图。实际完成：

1. 无模型配置时创建记录、导入横竖照片，关闭重开后中文搜索、打开、软删除和恢复；手工编册、保存预览、断网读取 HTML、下载完整 PDF（81,608 字节）。
2. 信件未提交草稿恢复、取消与确认封存、未到期正文和独占照片隔离、重启到期提醒、明确拆阅、首次阅读时间保留及删除恢复。
3. 键盘上下移动章节和内容块后焦点保留，选集按勾选顺序添加再排序，照片说明和来源不丢失；模板切换、保存后预览及 440px 窄窗口 iframe 无横向溢出。
4. Responses / Chat Completions 分别配置、四项能力测试、模型列表和启用切换；记录标题采用、主动补问、月报编辑和重新生成保留旧稿。
5. 受限工具 Agent、分月全年稿、采用前手工快照及恢复；恢复后正文、模板、封面、章节/照片顺序、说明与来源均匹配。版本恢复允许重新分配已被整册替换的内部章节 ID。
6. 无工具能力降级、取消能力测试、取消任务并重试，只保留一份最终草稿；创建/下载/恢复备份，再次停止重开。

最终保留 **2 条记录、2 张记录关联照片、1 本年册、8 份 AI 草稿、2 封信件**；其中一封已读、一封仍封存。封存信正文与独占照片恢复后依然不可读，记录共享照片仍可加载。临时服务与测试资料已清理，回执和导出保留在本机。

75% 及模型补修后的已有完整回归证据：`test-results/2026-09-12T10-30-38-837Z-55448/`。本轮旧测试在统一模板增加年度开篇后出现非唯一选择器，已改为按正文定位；排序后的手动稿检查使用稳定章节 ID，不再假设手动章节总排第一。失败回执保留，没有重放不确定的业务操作。

## HTML、字体与 PDF 排版

最终排版证据：`test-results/export-layout-1789216054006/`，包括 HTML、ZIP、PDF、生成回执、`pdf-inspection.json`、逐页 PNG 和总览。

- 预览、HTML 与 PDF 使用同一份冻结保存稿、完整照片及来源；异步导出不会混入后续修改。
- 照片版采用较大图片和先图后文，文字版采用衬线正文、较紧凑图片和先文后图；版心、字体与分页有实际差异。
- 所需 Noto Sans/Serif SC WOFF2 片段嵌入 HTML，ZIP 附 OFL 许可和版本清单。断网打开后中文、字体、横竖照片正常，无 CDN 依赖。
- Chrome 实际生成两套 PDF，指定 Edge 路径的打印测试也通过。使用独立 profile 禁网排版，等待字体/图片；取消、超时、无效浏览器路径及临时文件清理已测试。
- 照片版 **10 页**、文字版 **9 页**，用 PyMuPDF 渲染并打开全部 **19 页**，检查长中文跨页、横竖照片、EXIF、纯照片记录、空月份、说明和来源；中文可提取、字体全部嵌入，无空白尾页和文字越界。
- 第一批发现附页 class 导致空白尾页、选集标题孤立分页，修复后以上述第二批重新验收。旧 `export-layout-1789214934026` 不是最终通过证据。
- 两份 PDF 已作为实际浏览器标签打开，标题元数据正确。Tabbit 无法接管原生 PDF 阅读器，未得到阅读器完整截图；逐页检查采用独立 PDF 渲染结果，未宣称实体打印机实印。

## 视觉证据与捕获限制

`test-results/visual-final/visual-qa-report.json` 记录原色离线 HTML 检查：照片模板 **425×8676**、文字模板 **425×8945** 的完整 PNG 已打开，检查了中文长文、照片、说明与窄窗口留白。

测试只在自有页面加入 `darkreader-lock` 并移除 Dark Reader 注入样式，没有修改用户全局扩展设置。`fullPage:true, scale:'css'` 与临时稳定滚动条留白改善部分截图，但动态应用页仍可能超时或退回视口图。

回退图有缩放/裁切，**不算完整 UI 截图证据**。最终 E2E 的 4 张回退图均已打开查看，`fullPageScreenshotsComplete` 如实为 `false`。脚本记录截图前后 DOM 尺寸、DPR、PNG 尺寸与 `capturedFullPage`；仅对失败的纯截图重试，业务结果与截图覆盖状态分别保存。元素截图诊断也超时，见 `test-results/visual-final/letters-capture-diagnostic.json`。窄窗口和键盘交互用实际操作和 DOM 检查，不能等同于所有界面完整截图通过。

## 真实模型与已知边界

| 项目 | 验证范围或限制 |
|---|---|
| Codego | 前轮真实虚构标题与此前授权的失败标题请求重放成功；完整真实工具/Agent/年册质量未验收。旧失败无原始响应，具体根因不能断言 |
| AnyRouter | 模型列表成功，最小生成仍被网关额外 Codex 校验拒绝（HTTP 400 `invalid codex request`）；本轮未再次调用 |
| 本轮模型测试 | 本机模拟双协议、能力、错误、降级、任务和数据保护；没有索取 Key 或发送正式记录 |
| Windows 凭据 | 前轮用随机测试值实际写入、读回并删除；不可用时有明确的仅本次会话模式 |
| AI 事实质量 | 来源存在、范围和照片归属已校验，语义真实性仍需采用前核对 |
| PDF 环境 | 需要本机 Edge/Chrome/Chromium；缺少浏览器会明确失败，手动编册和 HTML 仍可用 |
| 备份规模 | ZIP 512 MB、解压 1 GB；更大资料需停止应用后完整复制 `data`。未自动清理未关联原图 |
| UI 完整截图 | Tabbit 的超时、缩放和视口回退仍未解决；导出 HTML 原色与 PDF 逐页检查已完成 |

详细协议诊断见 [MODEL_TROUBLESHOOTING.md](MODEL_TROUBLESHOOTING.md)，配置限制见 [AI_CONFIGURATION.md](AI_CONFIGURATION.md)。

## 正式服务与 Git 交付

正式服务已在确认本应用身份及没有待执行/执行中任务后，通过本应用启动器完成停止和启动，地址为 **http://127.0.0.1:4317**。迁移 005 已应用，前端和未来信接口可用；重启前后记录、照片关联、年册列表、AI 草稿、模型配置及历史任务的摘要完全一致，没有添加测试资料。证据：`test-results/final-service-upgrade.json`。

升级前一致性备份为 `data/backups/yearbook-20260912T140142009Z-d7d4a07f-9431-4de3-98a5-1b7a57694219.zip`（迁移 001–004，不含密钥）。本次只升级本应用资料，未恢复或覆盖正式数据。

源码、锁文件、迁移、Windows 脚本、测试与文档在本地 Git 交付检查点保存；没有远端推送。001–004 的原始字节、原 `img`、暂存范围和凭据字面值检查在提交前核对，回执为 `test-results/final-git-audit.json`。`data`、密钥、日志、运行状态、构建与测试生成物不入 Git。项目按约定在 100% 节点暂停，上述真实模型与截图环境边界保持明确。
