# 《一年一册》实施计划

## 工作约定

- 用户要求每完成整个项目约四分之一暂停。本项目按四个可验收里程碑计量，百分比代表交付节点，不代表代码行数或所有功能已完成。
- 2026-09-12 已从 75% 节点及模型补修继续完成最终交付，现于 **100% 节点暂停**。接续文档与真实验证边界已更新。
- 沿用用户已确认的实际工作区 `D:\项目文件夹\yearbook`，不搬迁到最初需求中的 C 盘路径。项目首次初始化时 Git 无提交；本次最终节点从 `2e7010b` 继续，保留 `img` 下三张用户图片和已有实现。
- 不索要或输出用户 API Key；真实连接诊断由本地服务读取设置中保存的凭据。所有自动化测试用独立临时目录，用户数据和生成物不入 Git。
- 主线负责架构、共享接口、集成、启动器和验收；按用户授权尝试多模型协作，实际参与情况见下文，不将模型供应失败计为成功协作。

## 四个交付节点

25%、50%、75% 保留各节点当时的交付范围；当前完成情况以 100% 节点及 [ACCEPTANCE.md](ACCEPTANCE.md) 为准。

### 25% · 本地记录与回顾闭环
- [x] 检查 Git、现有资产、父目录说明和开发环境。
- [x] 确认 Node.js 24 LTS；当前验证环境 24.14.1 / npm 11.17.0。
- [x] 阶段 1：React + Vite、Fastify、SQLite 迁移、共享校验、导航与本地运行。
- [x] 阶段 2：记录 CRUD、照片原图/方向/缩略图/去重、人物与标签、回顾补记。
- [x] 阶段 2：时间轴、月历、中文搜索、软删除与恢复。
- [x] 阶段 2：一致性备份、结构校验与失败回滚、新目录恢复验证。
- [x] Windows 启停脚本和离线核心资源。
- [x] 类型检查、构建、核心/接口测试、Tabbit 浏览器关键流程；该节点未保存截图，75% 节点已补充。
- [x] 形成 README、架构、验收、接续说明。

### 50% · 手动年册基础闭环（已完成，导出完善留至最终节点）
- [x] 阶段 3：同年多册、结构化章节/段落/图片与来源、章节排序、封面与两种模板设置；年册块/选集照片排序 UI 和模板导出差异仍待补全。
- [x] 预览、保存、重开、版本保护，手动制作并导出基础年册。
- [x] HTML 离线资源 ZIP 与浏览器打印 PDF 任务；任务状态、取消/重试、重启失败识别。
- [x] HTML 导出接口实际生成并下载 ZIP，75% 节点补充断网打开验证；PDF 路径已实现，尚无成功生成后实际打开及打印排版的验收结论。
- [x] 接口测试覆盖长文本契约、来源关联、软删除、版本和 HTML 导出。

### 75% · 模型接口与 AI 助理（已完成，按要求暂停）
- [x] 阶段 4：多配置、地址规范化、Windows 系统凭据/会话后备、两个独立协议适配器。
- [x] 分别测试文本/图片/工具/流式能力，模拟错误、降级和路由。
- [x] 阶段 5：标题、整理、提问、月报、分月全年编册、自然语言 Agent。
- [x] 范围受限工具、来源校验、幂等保存、草稿版本采用、持久任务/中断恢复/用量。
- [x] 迁移 003/004、旧版本备份升级、AI 来源/媒体关系校验、恢复后凭据隔离。
- [x] 任务代次、取消/重试交叠、恢复期间慢请求保护、导出生命周期回归。
- [x] 原 75% 节点全量测试 11 个文件 84 项通过；生产/开发脚本和端口冲突隔离验证通过。后续模型补修结果见下文。
- [x] 完整 Tabbit 浏览器闭环：无模型手工编册、断网 HTML、双协议能力、月报/Agent/全年稿、版本保护、取消重试、备份恢复和再次重启。
- [x] 保存实际截图与回执；Dark Reader 和截图裁切影响已明确记录，原色完整视觉验收留至最终节点。
- [x] 更新 README、架构、模型配置、验收和接续文档。
- [x] 建立本地 Git 检查点，排除资料、密钥、日志与生成物；暂存审查及迁移字节一致性检查通过。

本轮顺序与分工：

1. 核对现有实现、Git 和环境；重新读取官方 Responses 迁移与工具调用文档。
2. 按授权尝试多模型协作；Sol/Terra/GPT-5.5 的尝试因服务供应错误或不支持未完成任务。实际由主线实现协议/凭据/设置和集成，GPT-6 Astra 子代理实现 AI 流程/页面并参与并发与来源审查，不将失败尝试计作已完成协作。
3. 先验证本地模拟服务的双协议与独立能力，再串通记录整理、月报、全年编册和自然语言 Agent。
4. 执行类型检查、构建、隔离接口/核心测试和实际浏览器流程；保护原记录、手动稿和已有资料。
5. 更新架构、接口配置、验收与接续文档，在 75% 节点暂停。没有真实 API 时明确记录未验证，不索要密钥。

### 100% · 小功能与全量交付
- [x] 记忆盲盒与生活第一次基础功能已提前完成；最终节点做综合回归。
- [x] 阶段 6：给未来的信实体、写信/照片、到期可见、首页提醒及检索隔离；草稿/封存/拆阅/回收站与新目录恢复已实际测试。
- [x] 导出专用字体、两种模板差异、照片/内容块顺序、长中文分页、真实 PDF 打开与打印检查；修复空白尾页与选集标题分页，第二批共 19 页已渲染查看。
- [x] 综合异常恢复、启动端口冲突、中文与空格路径、多次备份迁移验收；Windows 完整中文空格项目路径内 11 个步骤通过。
- [x] 六个 npm 脚本、离线闭环、UI 交互/E2E/导出检查、文档交付；Tabbit 完整 UI 截图限制已单独记录。
- [x] 明确真实 API 的实际验证情况：Codego 前轮真实标题成功，完整真实 Agent/年册质量未验收；AnyRouter 最小生成仍被网关拒绝，本轮仅运行本机模拟测试。

本次最终节点实施顺序（2026-09-12）：

1. 已定位实际 Git 仓库 `D:\项目文件夹\yearbook`；起始提交 `2e7010b`，工作区干净，未配置远端。保留原资料，不迁移到不存在的旧 C 盘路径。
2. 按用户授权以多模型分工推进：未来信服务/迁移/隔离测试，导出字体/模板/PDF，以及年册排序交互；主线负责未来信界面、集成、浏览器/Windows 验收与文档。各自限定文件边界，统一集成验证。
3. 先建立未来信与现有资料隔离的完整闭环，同时补全可离线携带的字体、可区分模板、内容块和照片顺序。
4. 使用独立临时资料生成真实 HTML/PDF，打开、渲染并检查中文长文、横竖照片、纯图片记录及跨页；通过 Tabbit 执行关键用户流程。
5. 执行类型检查、构建、单元/接口/E2E，复核 Windows 启停及中文/空格项目路径，修复实际失败。
6. 更新 README、架构、模型能力边界、验收与接续说明，审查 Git 变更并建立本地交付检查点，不推送远端。

最终节点实测记录：

- 未来信专项 16 项通过；最初 114 项基线通过，集成第一轮为 17 文件 148 项通过，最终 `npm test` 为 **18 文件 149 项通过**，类型检查/构建通过。
- Windows Node 24.14.1 / npm 11.17.0，整个项目复制至系统临时中文空格目录，生产/开发/.cmd/端口冲突/停止隔离/重启保留共 11 步通过，证据 `test-results/runtime-final.json`。
- PDF 用 Node 原生 CDP 驱动独立 Chromium，实际 Chrome 双模板及 Edge 指定路径打印通过。第一批查出附页 class 与封面冲突导致空白尾页，已修正并重新检查。
- 完整浏览器业务流程通过、退出码 0：未来信、手动编册/离线 HTML/PDF、三层排序/键盘/窄窗口、双协议四能力、AI/Agent、版本保护、取消重试、备份恢复及再次重启。最终保留 2 条记录、1 本年册、8 份 AI 草稿和 2 封信。证据 `test-results/2026-09-12T13-53-48-977Z-65276/`。
- 原色离线 HTML 全页图与 PDF 全部 19 页已实际打开检查。动态 UI 的 Tabbit 截图仍会超时或回退裁切，单独记录限制，不将回退图算作完整截图。
- 正式服务已创建升级前一致性备份并由本应用启动器重启，当前地址 `http://127.0.0.1:4317`；迁移 005 生效，重启前后正式资料与模型配置摘要相同。源码和文档保存本地 Git 检查点，不推送；本轮在 100% 节点暂停。
- 按用户要求尝试 Sol 两次（供应高负载失败）、Terra（供应不支持），实际实现与验收由主线及可用 GPT-6 Astra 代理分工完成，不将失败调用列为成功的多模型成果。

## 75% 节点后补修：模型配置失败（2026-09-12）

本轮仅处理用户报告的模型配置问题，不推进最终节点。

- [x] 核对实际保存配置、凭据可用性、URL 规范化及真实最小请求；地址正确，服务返回 HTTP 400 `invalid_responses_request / invalid codex request`。
- [x] 确认能力验证没有沿用已启用的流式设置；核对官方协议与 DeepSeek Harness 的兼容、错误及 SSE 处理。
- [x] 修复能力验证请求设置、可诊断的安全错误分类、SSE 结束处理和非完成响应保护。
- [x] 使用保存配置复测；AnyRouter 仍返回 HTTP 400 `invalid codex request`，具体网关规则待服务方确认，真实生成未连通。没有猜测切换协议或模型。
- [x] 类型检查、构建、12 个文件 101 项测试和完整 Tabbit E2E 通过；实际设置页显示流式方式及具体错误，其余三项能力保持尚未验证。更新验收、诊断和接续文档。
- [x] 建立本地 Git 补修检查点，不推送远端；继续停在 75% 节点。

## 75% 节点后补修：Codego 的 HTTP 200 整理失败（2026-09-12）

- [x] 核对实际配置与两个失败任务：均使用 Codego，固定流程没有请求图片或工具；工具未验证不是直接原因。
- [x] 真实虚构素材及已授权失败标题请求重放均成功，且发生在补修之前。保留历史根因尚不能确认的结论，不将模型列表、能力探测或空错误对象缺陷直接等同于历史失败原因。
- [x] 修复成功响应携带空 `error` 的误判；HTTP 200 内的生成失败保留事件类型并使用准确分类。明确失败、取消、未完成响应继续拒绝。
- [x] 25 项协议回归与完整 HTTP/SQLite 草稿保护测试通过；全仓 12 文件 114 项、类型检查、构建通过，生产服务已重启。
- [x] 完整隔离 Tabbit 浏览器回归通过，截图已打开，恢复并重启后记录/年册/草稿保留；更新验收、诊断及接续文档。
- [x] 建立本地 Git 检查点；项目保持 75% 节点。

## 重要取舍

- SQLite 使用 `better-sqlite3`，迁移有版本与校验记录；单人本地服务绑定 127.0.0.1。
- 发生日期保存为 `YYYY-MM-DD` 或空值，创建/修改时间独立存 ISO 时间，不把日期转成 UTC。
- 图片按文件哈希去重，原图不可变；方向规范化的阅读图和缩略图为派生文件。
- 生产同源提供 UI/API。界面字体和图标随构建打包，不使用 CDN；导出按内容选择 Noto Sans/Serif SC WOFF2 字体片段，随 HTML 内嵌并随 PDF 嵌入，ZIP 包含 OFL 许可证。
- AI、年册、未来信在对应节点完成前不显示虚假的成功结果。

## 已核对文档

2026-09-12 通过 HTTP 200 读取以下官方资料：
- https://nodejs.org/en/about/previous-releases ：24 为 LTS。
- https://developers.openai.com/api/docs/guides/migrate-to-responses ：Responses `output` items 与 Chat `choices` 结构不同，存储策略需显式处理。
- https://developers.openai.com/api/docs/guides/function-calling ：模型实现阶段已再次核对；工具由应用执行、参数校验，Responses `call_id` / `function_call_output` 与 Chat `tool_call_id` 分别处理。
